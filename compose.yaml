services:
  watchpile:
    build: .
    image: watchpile-server:dev
    ports:
      - "3210:3210"
    environment:
      PUID: "1000"
      PGID: "1000"
      TZ: "America/Sao_Paulo"
    volumes:
      - watchpile-data:/data

volumes:
  watchpile-data:
